<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical" 
    android:background="@drawable/welcome_picture1"
    >

        <!-- <ProgressBar
            android:id="@+id/welcome_picture_img"
            android:layout_width="100dip"
            android:layout_height="150dip"
            android:layout_centerInParent="true"
            android:indeterminateDrawable="@drawable/page_loading" />
        <TextView
            android:id="@+id/welcome_message_text"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/welcome_picture_img"
            android:layout_marginTop="20dp"
            android:gravity="center"
            android:paddingLeft="25.0dip"
            android:paddingRight="25.0dip"
            android:text="小驴正在努力为您加载，请稍候~"
            android:textColor="@android:color/black" /> -->
        
</RelativeLayout>